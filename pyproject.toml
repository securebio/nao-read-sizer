[project]
name = "read-sizer-pipeline"
version = "1.0.0"
requires-python = ">=3.8"
dependencies = [
    "boto3>=1.26.0",
]

[project.optional-dependencies]
test = [
    "pytest>=7.0.0",
    "pytest-mock>=3.10.0",
]
